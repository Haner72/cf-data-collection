/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DataInsertion } from './data-insertion.component';

describe('DataInsertionComponent', () => {
  let component: DataInsertion;
  let fixture: ComponentFixture<DataInsertion>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [DataInsertion]
    })
    .compileComponents();

    fixture = TestBed.createComponent(DataInsertion);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
